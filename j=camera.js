document.getElementById('photoForm').addEventListener('submit', function(event) {
    event.preventDefault();

    // Add logic to handle photo submission
    alert('Photo submitted successfully!');
});